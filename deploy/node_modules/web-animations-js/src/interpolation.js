(function(scope,testing){function interpolate(from,to,f){if("number"==typeof from&&"number"==typeof to){return from*(1-f)+to*f}if("boolean"==typeof from&&"boolean"==typeof to){return .5>f?from:to}WEB_ANIMATIONS_TESTING&&console.assert(Array.isArray(from)&&Array.isArray(to),"If interpolation arguments are not numbers or bools they must be arrays");if(from.length==to.length){for(var r=[],i=0;i<from.length;i++){r.push(interpolate(from[i],to[i],f))}return r}throw"Mismatched interpolation arguments "+from+":"+to}scope.Interpolation=function(from,to,convertToString){return function(f){return convertToString(interpolate(from,to,f))}};if(WEB_ANIMATIONS_TESTING){testing.interpolate=interpolate}})(webAnimations1,webAnimationsTesting);