// Test component for storybook-check workflow validation
// This file should NOT be detected by the workflow (react path should be excluded)
const TestComponentForWorkflow = () => {
  return <div>Test Component in React</div>;
};

export default TestComponentForWorkflow;
