# Astryx migration — running QA findings

Live ledger for QA on `to-astryx`, in the finding format of
`REGRESSION-CATALOG.md` §0 (fix-mechanism class **T** theme / **L** layout
composition / **C** component work / **F** in flight elsewhere; severity
High / Med / Low).

The catalog is a **frozen** audit — it pins the branch SHA, the baseline, the
viewport, and carries a reproduction procedure (§6) that regenerates its own
numbers. Appending to it would destroy that. This file is the open one: rows
are appended, then marked `FIXED <sha>` or `PROMOTED FR-####` in place — never
deleted, so a re-opened defect is visible as a second row citing the first.

- Intake, measurement and de-duplication: `.claude/skills/astryx-qa-finding/SKILL.md`
- Implementation and verification: `.claude/skills/astryx-migration-fix/SKILL.md`

IDs are `Q-<n>`, sequential, never reused. They do not collide with the
catalog's prefixes (`G-` global tokens, `S-` app shell, `T-` table pages,
`O-` overlays, `F-` forms & controls, `R-` per-route). A row that tracks a
catalog finding cites it: `Q-7 (tracks G-3)`.

Baseline for every measurement unless the row says otherwise: viewport
**1600×1000**, both modes, dark entered through the header `Dark mode` button
with `document.documentElement.dataset.theme` asserted `dark`. Legacy values
are derived from `git show origin/main:<path>`, the measured parity tables in
`packages/backend.ai-ui/src/theme-shim/`, or antd 6.5.0's own formulas — the
legacy build cannot be run on this branch.

| # | What / where | Legacy expected | Measured current | Class | Sev | Status / fix |
|---|---|---|---|---|---|---|
| **Q-1** | **Typing into any `rules`-bearing field loses focus after the FIRST character** (`react/src/components/astryxFormControls.tsx`, `useFormControlStatusProps`). Reported on the login endpoint field (`LoginFormPanel.tsx` `api_endpoint`, rule `pattern:/^https?:\/\//`), but the mechanism is app-wide: every Astryx control built on `Field` (TextInput, TextArea, NumberInput, Selector, MultiSelector, RadioList, Tokenizer). Light + dark, 1600×1000 | antd's controls read `FormItemInputContext` and repainted in place; the `<input>` node was never replaced, so focus and the caret survived validation | char 1 lands, then the `<input>` DOM node is REPLACED (tagged-node probe: `tagPresent:false`) and `document.activeElement` falls back to `<body>`; chars 2..n go nowhere — value froze at `"h"` after typing `http://abc`. Mechanism: the hook returned `{}` while pristine and `{statusVariant:'detached'}` on error, and Astryx `Field` wraps `{children}` in an extra `<div>` only when `statusVariant==='attached'` (its default) → element-structure swap → React unmount/remount. `.scratch/astryx-migration/qa3-remount-trace.mjs` shows `astryx-field` removing `DIV#A2` and adding a fresh `astryx-text-input` | **C** | **High** | FIXED — `statusVariant` pinned constant `'detached'`; `Switch`/`CheckboxInput` (not `Field`-based, no `statusVariant` prop) moved to a new `useFormControlStatusOnly()`. Regression test: `astryxFormControls.parity.test.tsx` › "the control is not remounted when its status changes" (2 cases; both fail on the old hook) |
| **Q-2** | **Login screen rhythm** — the modal body is padded twice (`react/src/components/LoginFormPanel.tsx`), the logo overflows its header line box, and the "Advanced" disclosure link inherits the dialog's 16px base. Light + dark, 1600×1000 | antd 6.5.0 oracle (rebuilt outside the repo from `origin/main`'s markup + `BAIModal`'s style defaults + `resources/theme.json` seeds): dialog **400×295**, form **352** wide at **24px** from each edge, form top **81**, bottom pad **20**, logo top **19**, logo→form **27**, Advanced link **14px/22px** | before: dialog **400×324**, form **304** wide at **48px** (Astryx `LayoutContent` already pays the 24px, so `styles.body={padding:paddingLG}` became additive), form top **87**, bottom pad **40**, logo top **12** (35px `<img>` inline in a 27px line box), logo→form **40**, Advanced link **16px/24px**. `/tmp/login-before-light.json` vs `/tmp/oracle-light.json` | **L** | **High** | FIXED — drop `styles.body` padding (the layout slot owns it); logo title becomes a flex row + `marginTop: var(--spacing-1)` to reclaim `DialogHeader`'s text-optical `marginBlock:-4px`; `Link isStandalone` and `Text type="inherit"`. After: 400×292 / 352 @ 24 / top 79 / bottom 16 / logo top 16 / logo→form 28 / 14px/21.9996px — every box within 4px of legacy |
| **Q-3** (regresses `issues/p3-w3b.md` §D1) | **Every floating surface the header band opens is pinned dark in BOTH modes** (`react/src/components/BAINotificationButton.tsx`, `UserDropdownMenu.tsx`). `MediaTheme mode="dark"` wraps the whole `Tooltip` / `DropdownMenu`, but both render trigger and `[popover]` panel as SIBLINGS, so the panel inherits `color-scheme: dark` + the on-dark tokens. Light + dark, 1600×1000 | legacy `ReverseThemeProvider` inverted against the app mode; QA directive is stronger — band chrome on-dark, floating surfaces resolve the APP mode (as `ProjectSelect`'s panel already does) | bell tooltip `bg rgb(255,255,255)` with `--color-text-primary` forced `#ffffff` → the `]` `Kbd` is **white on white in both modes** (Astryx's tooltip surface is deliberately inverted, so the forced scheme flipped it to white); user menu `bg rgb(48,48,48)` / white text in LIGHT mode. `/tmp/header-before-{light,dark}.json` | **C** | **High** | FIXED — on-dark context moved onto the trigger element (`data-astryx-media="dark"`, which IS what `MediaTheme` renders; `BaseProps` admits `data-*`), tooltip CONTENT wrapped in `MediaTheme mode={opposite of app}` per `SIDER-FIXES.md` §2. After: tooltip `rgb(20,20,20)`/white in light, `rgb(255,255,255)`/`rgb(23,23,23)` in dark; user menu `rgb(255,255,255)`/`rgb(20,20,20)` in light, `rgb(48,48,48)`/white in dark; band `rgb(255,255,255)` on `rgb(255,151,41)`/`rgb(232,138,40)` unchanged; the three user-menu dialogs still page-mode (p3-w3b holds) |
| **Q-4** (regresses `Q-2`) | **Login screen logo is centred; legacy had it LEFT-aligned** (`react/src/components/LoginFormPanel.tsx`, the `BAIModal` `title` slot). Light + dark, 1600×1000. Reported as "왜 가운데 정렬로 바꿨나요". Note: the SIDER logo alignment fix (`ac3f7d76d` / POLISH-3, a `:has(.logo-collapsed:not([hidden]))` selector in `BAISider.css`) is a different surface and shares no CSS with this one — verified by grep | antd 6.5.0 oracle (`.scratch/astryx-migration/qa4-oracle-logo.mjs` against `/home/ubuntu/.claude/jobs/19652f20/tmp/antd-oracle`, which mirrors `origin/main`'s title markup + `BAIModal`'s `styles.header`): logo **x = 24** from the dialog's left edge — the header's own 24px padding, and the SAME x as the form below it — right gap **162.5**, `centred: false`, logo y **19**. The legacy source *reads* `<div style={{ textAlign: 'center' }}>`, but that rule never rendered: `.ant-modal-title` is a `flex: 0 1 auto` item of the `display:flex; justify-content:space-between` header, so it shrink-wrapped to the image's own **213.5px** and centring a box around its only child is a no-op | before: logo **x = 93**, right gap **93**, `centred: true`, both modes (`/tmp/qa4-live-login-{light,dark}.json`). Mechanism: Astryx's `DialogHeader` gives the title slot the **full 352px** content width, so the inert antd-era declaration became a live one. `42e2e8c31` (Q-2) did not introduce the centring — it re-expressed `textAlign:center` as `justifyContent:'center'` while fixing the 35px logo's line-box overflow, faithfully preserving the *source* but not the *rendering* | **L** | Med | FIXED — `justifyContent: 'flex-start'` on the same flex row (Q-2's line-box and `marginTop` reasoning is untouched). After: x **24**, right gap **162**, `centred: false`, `logo.x === form.x`, both modes. Residue: logo y **16** vs legacy **19** — Q-2's deliberate `marginTop: var(--spacing-1)` trade-off that holds the logo→form gap at 28 vs legacy 27; not re-opened for 3px |
| **Q-5** | **Header user-name dropdown panel is denser/wider than legacy and CLIPS its own last row** (`react/src/components/UserDropdownMenu.tsx` + the Astryx `DropdownMenu` component defaults). Light + dark, 1600×1000. Reported as "Astryx 기본 스타일 때문인가요?" — yes, all five deltas are Astryx component defaults, not call-site choices | antd 6.5.0 oracle (`qa4-menu-metrics.mjs ENGINE=antd`, `menu.jsx` mirrors `origin/main:UserDropdownMenu.tsx` verbatim, same item strings as the live app): panel **181.4px** wide (shrink-wrapped under `styles.root.maxWidth:300`), **no** height cap (content 314px, does not scroll), item padding **5px 12px** → **32px** rows, leading **22px**, row gap **0**, icon x **16** / label x **38** from the panel edge | before: panel **300px** FIXED (`menuWidth={300}`) — ~120px of empty gutter; `max-height: 300px` with 352px of content, so the panel **SCROLLED and hid "Log Out"**; item padding **6px 8px** → **34px** rows, leading **21px**, row gap **2px**, icon x **12** / label x **34**. Already-matching and untouched: radius 8/4, panel padding 4px, bg, icon 14px, icon→label gap 8px, divider h1 / `4px 0` margin / 4px insets, 4px offset from trigger, right-edge alignment delta 0 (`/tmp/qa4-menu-{antd,before}-light.json`) | **T** / L | **High** (a hidden Log Out row) | FIXED — theme-first per the standing rule: `ANTD_DROPDOWN_DENSITY` in `backendAiTheme.ts` (`components: {'dropdown-menu': {gap:0, maxHeight:'none'}, 'dropdown-menu-item': {padding:'5px 12px', lineHeight:'22px'}}`, `THEME_NAME_REV` 8→9, artifacts regenerated) — the two keys `astryx component DropdownMenu` documents, so it fixes ~40 other menus with the same drift; `maxHeight:'none'` closes catalog **O-16** ("lift the cap if it bites" — it bit). Width is genuinely a call-site prop, so `menuWidth={300}` → `menuWidth="fit-content(300px)"` = legacy's `min(max-content, 300px)` CAP. After (both modes): every row in the table matches legacy — 32px items, `5px 12px`, 22px leading, gap 0, icon x 16 / label x 38, no scroll, panel 175 vs 181.4 (font-metric difference between the oracle's default sans and the app's theme face). Panel still resolves the APP mode (`rgb(255,255,255)` light / `rgb(31,31,31)` dark) so Q-3 holds. Residue: (a) popover **shadow** is Astryx's `--shadow-*` recipe, not antd's `boxShadowSecondary` — a global popover token that would move every floating surface, out of scope here and already tracked as catalog **G-6**; (b) Astryx dims a disabled row with `opacity: 0.5` where antd used `colorTextDisabled` (`rgba(0,0,0,.25)`), and legacy's per-item `style={{color: colorText}}` on the user-name row has **no** Astryx equivalent (`DropdownMenuOption` carries no style passthrough, P5 closed enums) — the closest composition is the current disabled-item mapping, kept |
| **Q-6** | **Sider menu items render an 8px rounded rectangle instead of the 20px pill — in the PRODUCTION build only** (`react/src/astryx-theme/backendAiTheme.ts` `SIDE_NAV_DENSITY['side-nav-item'].borderRadius` vs the compiled Astryx StyleX). Every route, both modes, expanded + collapsed, brand + admin scopes. Reported as "사이더 메뉴의 border radius도 달라졌고요" | antd `BAIMenu` set `Menu.itemBorderRadius: 20` on a 40px row. antd 6.5.0 oracle (`.scratch/astryx-migration/q67-oracle.mjs`, `src/tabsider.jsx` mirrors `origin/main:BAIMenu.tsx` verbatim): `li.ant-menu-item` `border-radius: 20px`, height 40, font-size 16 — a full stadium | `pnpm dev`: **20px** ✅. `pnpm run build` + `serve build/web`: **8px** ❌ — 22/22 route×mode combinations, `selected` and `rest` alike. Rule census on the built page (`q67-rules.mjs`): the theme's `.astryx-side-nav-item{border-radius:20px}` sits in `(unlayered)>astryx-theme`, but the entry stylesheet also carries an **unlayered** `.xh6dtrn:not(#\#){border-radius:var(--radius-element)}` — a SECOND copy of `SideNavItem`'s own StyleX. Cause: Astryx's `dist/**` is not StyleX-precompiled (`dist/SideNav/SideNavItem.js` still imports `@stylexjs/stylex`), so rollup runs it through `@stylexjs/unplugin`, which is `useCSSLayers: false` — and unlayered CSS outranks every named layer, so `@layer astryx-theme` can never win. Dev is unaffected because the dep pipeline never re-emits that CSS. `shots/tab-sider-restore/{before,prod}-measurements.json` | **T** → **C** (theme value kept, application moved) | **Med** | FIXED — `.bai-sider .astryx-side-nav-item { border-radius: 20px !important }` in `react/src/components/BAISider.css`, after the two cheaper levers were each written, BUILT and re-measured rather than reasoned about: **(a) specificity cannot win** — `:not()` takes its argument's specificity and StyleX pads with `:not(#\#)`, so `.xh6dtrn:not(#\#)` is ID-weight **(1,1,0)**; a (0,3,0) `.astryx-side-nav.bai-sider .astryx-side-nav-item` rule built clean and still measured 8px. **(b) `xstyle` does not reach the element** — it would have been the engine-native fix (StyleX resolves per atomic property at merge time, no cascade involved), but `SideNavItem` spreads `xstyle` onto its outer wrapper (`stylex.props(styles.root, xstyle)`) while the radius lives on the inner nav element built from `themeProps('side-nav-item') + stylex.props(navItemStyles.item, …)`, which accepts no caller styles; also built and measured at 8px. `SideNavItem` exposes no `className` passthrough either. Importance is the one axis that outranks an unlayered ID-weight declaration, and it is narrow here: one property, scoped to `.bai-sider`. The theme declaration STAYS (canonical default, still what Storybook renders) with a ⚠ pointer to the restatement. **No `THEME_NAME_REV` bump**: the recipe's emitted values are unchanged, only comments — `astryx theme build --check` still passes and the r9 artifacts are untouched. Blast radius measured, not assumed: a dev-vs-prod census of every `components:` key the brand theme declares (`side-nav`, `side-nav-item`, `side-nav-section`, `card`, `section`, `dropdown-menu`, `dropdown-menu-item`, `dialog`, `progressbar` — `q67-census.mjs`) found **this one key** differing and no other, so Q-5's dropdown density and the approved-1 dialog pin are genuinely reaching production. After: 20px in dev AND prod, 22/22 combinations, 0 pageErrors |
| **Q-7** | **Card-type tab chrome (`packages/backend.ai-ui/src/components/BAITabList.css`, `type="card"`) reported as "primary 컬러로 border를 그려주기위해 만들었던 tab 스타일이 최근 수정에서 의도하지 않게 변경되었습니다"** — investigated on all 7 card-tab surfaces (the 6 legacy `BAITabs` sites + `FolderExplorerModalV2`'s `xl` branch), both modes, dev AND production build | antd 6.5.0 oracle mirroring `origin/main:BAITabs.tsx`'s NEO override (`q67-oracle.mjs`): nav rail `1px solid #FF7A00`, inactive tab bottom border `#FF7A00`, active tab border t/l/r `#FF7A00` with `border-bottom-color: colorBgContainer`, radius 8px, padding-inline 16, inter-tab gap 2 | **NO DEFECT FOUND — the primary-colour border chrome is intact and unchanged.** Rail `1px solid rgb(255,122,0)` (admin `rgb(2,141,242)`, dark `rgb(190,94,6)`), active border t/l/r accent + bottom knocked out to the card surface, inactive bottom border accent, radius `8px 8px`, padding `16/16`, gap 2, height 36 — **identical** to the qa2-a baseline (`shots/qa2-a/final-measurements.json`, 22/22 route×mode combos, 0 field diffs) and **identical** between dev and the production build, hover included. Pixel-diffing the qa2-a `final-*` screenshots against today's shows the rail carrying the same ~1200 accent pixels on the same scanline. What DID move since qa2-a, and is the only later change reaching this surface, is the INACTIVE LABEL colour: `787d4e4d9` (approved-1) repinned `--color-text-secondary` `#51443C/#B8A89F` → `rgba(0,0,0,.65)/rgba(255,255,255,.65)`, so inactive tab labels read lighter. The oracle says legacy used `colorText` (`rgb(20,20,20)`) there, i.e. neither value matches legacy and the qa2-a choice of `--color-text-secondary` is the actual gap | **T** | Low | OPEN (not fixed here) — no border/radius regression exists to revert; repainting the inactive label is a separate, deliberate change that would diverge from the `--color-text-secondary` semantic qa2-a chose, so it is recorded rather than guessed at. Reproduction and probes: `q67-replay-qa2a.mjs`, `q67-measure.mjs`, `q67-explorer.mjs`, `q67-hover.mjs`, `q67-oracle.mjs`; shots under `shots/tab-sider-restore/` |
| **Q-8** | **Dark-mode hover on a FILLED button drops the brand hue entirely** (`react/src/astryx-theme/backendAiTheme.ts`, `--color-overlay-hover`). Reported as "다크모드에서 버튼 hover 시 같은 색 계열을 유지한 채로 변했으면 좋겠습니다. 현재는 평상시와 hover 했을 때의 색의 대비가 너무 많이 납니다" with a rest/hover pair of the folder-explorer `Create Folder` button. Every `variant="primary"` / `variant="destructive"` button, app-wide. Dark; light is affected in direction only | antd 6.5.0 oracle over the shipped `resources/theme.json` seeds (rebuilt outside the repo — antd is not a dependency on this branch): a filled button hovers to `colorPrimaryHover` `#ff9729` / `#d37f25`, i.e. **lighter, same hue**, in both schemes. `colorBgTextHover` was never composited over a filled surface — antd used it only for text/ghost buttons and menu rows | Astryx paints hover as `background-image: linear-gradient(var(--color-overlay-hover), var(--color-overlay-hover))` OVER the button's own fill. `--color-overlay-hover` is `rgba(0,0,0,0.06)` light (a 6% wash — harmless) but **opaque `#262626`** dark, because `ANTD_NEUTRAL_SURFACES` pins it to antd's `colorBgTextHover`, which `resources/theme.json`'s `colorFillSecondary: '#262626'` dark seed makes opaque. Composited, the gradient REPLACES `#be5e06` with flat grey. Measured `linear-gradient(rgb(38,38,38), rgb(38,38,38))` (`qa8/probe-hover.mjs`) | **T** | **High** | FIXED — `ANTD_HOVER_PARITY` in `backendAiTheme.ts`: `button` `variant:primary` / `variant:destructive` set `--color-overlay-hover: rgba(255,255,255,0.16)`, so hover LIGHTENS within the hue in both modes. The global token is untouched (it is antd-correct for the ghost/menu surfaces that are most of its call sites, and catalog G-4 pinned the opaque dark value on purpose). `THEME_NAME_REV` 9→10, artifacts regenerated, r9 deleted. PILOT-DECISION: a white wash rather than antd's exact `colorPrimaryHover`, which antd derives from its palette generator — re-implementing that is the only way to keep it correct under a runtime `theme.json` rebrand. Residue vs antd: primary light `#ff8f29` vs `#ff9729`, dark `#c87830` vs `#d37f25`; destructive light `#ff7d7f` vs `#ff7875`, dark `#c86c6e` vs `#d36664` — same hue, one perceptual step short of antd's lift |
| **Q-9** | **Hovering a tab in dark mode erases its label** (same `--color-overlay-hover` mechanism as Q-8). Reported as "멀티탭을 갖고 있는 statistics, admin users page 같은 페이지들에서, 탭을 hover 했을 때 다크모드에서 글씨가 안보입니다" with a shot of the hovered `Users` tab as a bare grey pill. Every `TabList` line-variant strip; the card variant was already immune | antd's `.ant-tabs-tab:hover` sets `color` only — **no hover background at all** | NOT a contrast problem: the label's computed `color` stayed `rgb(255,255,255)` while nothing rendered. `astryx-tab`'s first child is an ABSOLUTELY-POSITIONED overlay span painted with `--color-overlay-hover`; the label lives in a `position: static` sibling. A positioned box with `z-index:auto` paints in the positioned phase, i.e. ABOVE static in-flow text — so the opaque dark `#262626` covers the label outright, while 6% black in light lets it through. Measured `bg rgb(38,38,38)` on `tab>0` with the label at `tab>1` (`qa8/probe-tab-overlay.mjs`), and reproduced pixel-for-pixel against the report (`qa8/before-tabs-admin-users-dark.png`) | **T** | **High** | FIXED — `ANTD_HOVER_PARITY` sets `tab.base { --color-overlay-hover: transparent }`, removing the pill and leaving Astryx's own accent recolour, which is antd's behaviour exactly. `BAITabList.css` already did this for the card variant; this generalises it to the line variant where the defect was reported. After: overlay `rgba(0,0,0,0)`, label visible, accent recolour intact in both modes (`qa8/after-tabs-admin-users-dark.png`) |
| **Q-10** | **The tooltip bubble is WHITE in dark mode** (`react/src/astryx-theme/backendAiTheme.ts` + the two tooltips that hand-compensated for it). Reported as "다크모드에서도 툴팁은 검은색 배경을 유지합니다. 폰트색은 흰색 계열", and visible in the session-drawer `Detailed Information` shot. All 83 `Tooltip` call sites | antd `colorBgSpotlight` — `rgba(0,0,0,0.85)` / `#424242`, i.e. a DARK bubble with `colorTextLightSolid` (`#fff`) copy in **both** schemes. It is one of the few antd tokens that is deliberately not an inversion | light `rgb(20,20,20)`/white ✅ but dark `rgb(255,255,255)`/`rgb(20,20,20)` ❌ — Astryx inverts the tooltip against the app surface, so dark mode flipped it white (`qa8/probe-tokens.mjs`) | **T** | **High** | FIXED — `ANTD_HOVER_PARITY` pins `tooltip.base` to `light-dark(rgba(0,0,0,0.85), #424242)` with `#FFFFFF` copy. Because the surface no longer flips, the two call sites that compensated for the inversion — `SiderToggleButton.tsx` and `BAINotificationButton.tsx`, both wrapping tooltip CONTENT in `MediaTheme mode={opposite of app}` per Q-3 — become the constant `mode="dark"`; their now-unused `useTheme()` / `useThemeMode()` reads are dropped. After: `rgba(0,0,0,0.85)`/white light, `rgb(66,66,66)`/white dark — both exactly the antd oracle. Q-3's header-band work is unaffected (the trigger keeps its own `data-astryx-media="dark"`) |
| **Q-11** | **The click-to-copy control reads as oversized** (`react/src/components/astryx-bui/BAICopyableText.tsx`). Reported as "복사 아이콘 크기 짱 크네요" with no surface named. Every copyable value (image list, session detail, RBAC drawer, vfolder description) | antd `Typography.Text copyable` drew a BARE anticon at `1em` of the surrounding text — **14px**, inline, `margin-left: 4px`, with no control box | glyph **16×16** inside a **24×24** `IconButton size="sm"`, beside 14px text (`qa8/probe-copy.mjs`, 4 rows on `/environment`). So two separate deltas: +2px on the glyph, and a 24px hit box antd did not have | **C** | Low | **OPEN — reported, not fixed.** The 24px box is a recorded PILOT-DECISION in the component header (ticket-08 made the bare `<svg role="button">` a real `IconButton` for the focus ring and hover surface, explicitly accepting the larger footprint); removing it would undo an a11y fix, which is a team decision rather than a QA correction. The glyph is not reachable from props either — `IconButton` exposes no glyph-size knob and its StyleX `width/height` beats lucide's `size` attribute (tried `size="1em"`, measured no change at 16px), so 14px would need a scoped CSS override. Both halves are judgement calls, so the measurements are recorded and the choice left open |
| **Q-12** | **Column keys read `[object Object]` in the table-settings modal** (`packages/backend.ai-ui/src/components/Table/BAITableAstryx.tsx`). Reported as "Deployments page의 table settings modal 에서 [object Object] 키로 보이는 문제". Affects the CSV-export modal identically — the two share the label expression | antd's `BAITableSettingModal` rendered the column's `title` NODE straight into its row, so an element header displayed as itself; there was no string conversion to get wrong | Both modals need a `string`, and both took it with `String(renderTitle(column) ?? key)`. A `title` is a `ReactNode`, so any header that is an element rather than a bare string — a tooltip'd header, a unit suffix, a sort affordance — hit `String(reactElement)` and produced the literal `"[object Object]"`. Grouped columns additionally lost their group, so two `CPU` children under different parents were indistinguishable | **C** | **Med** | FIXED — `titleToPlainText()` walks the rendered node (string / number / array / fragment / element `props.children`) down to text, and `columnPlainLabel()` composes `"<group> / <own>"`, falling back to the column key when a header is genuinely textless (icon-only). Both call sites — the settings modal and the CSV-export column list — now go through it. Regression test `BAITableAstryx.columnLabel.test.tsx`, 6 cases driven through the real settings modal (element, plain string, fragment, function-as-title, nested-under-group, textless) |
| **Q-13** | **Two different column-settings modals in one app** (`react/src/components/TableColumnsSettingModal.tsx` vs `packages/backend.ai-ui/src/components/Table/BAITableAstryxSettingModal.tsx`). Reported as "My Environments, RBAC Mangement 의 table settings modal 에서만 다른 스타일을 갖고 있습니다. 다른 컴포넌트와 동일한 스타일을 유지하도록 수정이 필요합니다". Five tables reach the odd one: `RoleNodes` (RBAC Management), `CustomizedImageList` (My Environments), `ImageList`, `ContainerRegistryList`, `AgentSummaryList` | One look for every table. antd had a single `BAITableSettingModal` | Two implementations. Most tables get `BAITableAstryx`'s `tableSettings` modal — `Dialog` 420px, title + subtitle header, unlabelled search, drag handles, locked required columns, Cancel/Apply. The five above render a `BAIModal` + `Form` with a LABELLED search field, a fixed 220px box, no reordering, OK/Cancel. Same job, visibly different modal. The legacy one also flattened titles with a local `onChangeTitleToString` that picked only DIRECT string children, so a header nesting its text one level deeper rendered its raw column key — Q-12's defect, one narrower | **C** | Med | FIXED — `TableColumnsSettingModal` keeps its prop contract and its `useHiddenColumnKeysSetting` persistence but DELEGATES rendering to `BAITableAstryxSettingModal` (now exported from BUI). Storage is untouched on purpose: those five store `hiddenColumnKeys`, the built-in prop stores a richer `columnOverrides` (hidden + order + width), and migrating would drop users' saved columns on first load. Two behaviours deliberately not forwarded — `disableReorder` (there is nowhere in `hiddenColumnKeys` to put an order, so one the user set would be silently lost) and required-column locking (no call site sets `required`). Label flattening now uses the shared `columnTitleToPlainText` / `renderColumnTitle`, moved to the engine-neutral `tableTypes`. After (`qa8/probe-settings-modal.mjs`): agents / RBAC / My Environments all 420px, `Table Settings`, same search placeholder, Cancel+Apply; grip handles 9 / 0 / 0 as designed |
| **Q-14** | **The row-selection checkbox overhangs its own cell, so a bleeding table's first row does not line up with the card above it** (`packages/backend.ai-ui/src/components/Table/BAITableAstryx.tsx`). Raised by the author alongside the deliberate full-bleed decision: "카드 안의 테이블이 있을 경우 좌우 패딩을 주지않고 카드를 가득차게 한거는 의도한겁니다 … 대신에 첫 row의 시작점은 다듬어야하지만". Every table with `rowSelection` (sessions, admin users, …); tables without one were already correct | The first column's content starts at the card's own content edge, so the table's first row lines up with the card title and filter row above it | Card content edge **287**. A table WITHOUT selection starts its first cell's content at **288** ✅ (cell box 264 + Astryx's 24px first-column inset). A table WITH selection puts the checkbox at **280** ❌ — 7px left of the card edge and 8px left of its own siblings. Mechanism (`qa8/probe-selcell.mjs`): the selection plugin injects its column with no width, so it lands at the 36px default; the first-column inset takes 24 and the trailing pad 8, leaving a **4px** content box for a **20px** checkbox, which the cell's `justify-content: center` wrapper then overhangs by 8px each side | **C** | Med | FIXED — a local `selectionWidth` plugin sets `width: pixel(52)` on the injected `__xds_selection` column (24 inset + 20 checkbox + 8 trailing pad). It runs immediately after the selection plugin, on the column list that plugin has already injected into. `useTableSelection` exposes no width option (`astryx component useTableSelection` — the props are all selection callbacks), and Astryx's own `transformColumns` pipeline hook is the sanctioned way to adjust an injected column. After: checkbox left **288** on sessions, identical to the non-selection table's first column and to the card content edge |
| **Q-15** | **The same semantic action has two colours** (`packages/backend.ai-ui/src/components/Table/BAINameActionCell.{tsx,css}`). Reported as "BAINameActionCellAction 에서 보여주는 control button 의 색과 다중 체크 박스를 눌렀을 때 나오는 컨트롤 버튼의 색상이 다름". 45 call sites in 39 files consume the class pair, 28 with `type: 'danger'` | antd `Button type="text"` + `token.colorInfo` / `colorError` — **`#028DF2` / `#0387BF`** and **`#FF4D4F` / `#BE3D3F`** (oracle over the shipped `resources/theme.json` seeds) | Row buttons **`#00458C` / `#C7D3FF`** and **`#89001A` / `#FFC6C1`**, while the bulk-selection buttons on the SAME table kept their inline `token.colorInfo` and stayed `#028DF2`. Mechanism: `BAINameActionCell.css` remapped antd's tokens onto Astryx's hue-family TEXT tiers (`--color-text-blue` / `--color-text-red`), which are ~4 ramp steps darker and correspond to no antd token — an unmeasured substitution, not a measured parity value | **C** | **Med** | FIXED — the component publishes `--bai-nac-{info,info-bg,error,error-bg}` on the actions container straight from the theme shim's measured antd values, and the CSS reads those. Row and bulk buttons now both measure `rgb(2,141,242)`. ACCESSIBILITY RESIDUE, recorded not hidden: on white `#00458C` is 8.9:1 and antd's `#028DF2` is 3.3:1, on icon-only controls — parity is what the report and the project directive ask for, and the CSS block names the one place to change if the team prefers the darker tier. **The overflow-menu half of the report is NOT fixed** — see Q-15b |
| **Q-15b** | **Destructive rows in the overflow (…) menu render in the default colour** (`BAINameActionCell.tsx`). Reported alongside Q-15 and explicitly marked optional by the reporter ("이건 선택사항") | antd set `danger: action.type === 'danger'` on the menu item → `#FF4D4F` / `#BE3D3F` (`git show origin/main:…/BAINameActionCell.tsx`) | measured `#141414` / `#FFFFFF`, byte-identical to the non-destructive rows | **C** | Low | **OPEN — reported, not fixed.** The existing PILOT-DECISION said `DropdownMenuItemData` has no `danger` flag; that is true, and its `label` is additionally typed `string`, so the data mode cannot carry a coloured node — verified by the compiler. The colour IS reachable through `DropdownMenu`'s COMPOUND mode (`DropdownMenuItem` takes `label: ReactNode` + `style`), but that means rewriting this menu's whole render path (data `items` → children) and carrying the divider, disabled and keyboard behaviour across with it, for a change the reporter marked optional. Two intermediate attempts are recorded in the code comment: a `var(--bai-nac-error)` label span measured `rgb(20,20,20)` because Astryx renders the menu panel as a SIBLING of the trigger, so it inherits none of the container's custom properties |
| **Q-16** | **The E-Mail column is too narrow for its own action buttons** (`react/src/components/AdminUserManagement.tsx`). Reported as "default email column width 가 좁아서 컨트롤 버튼이 모두 보이지 않음" | The antd table ran `scroll={{ x: 'max-content' }}`, which puts rc-table in `auto` layout, so the column sized to its content — email + every action button (`git show origin/main:packages/backend.ai-ui/src/components/BAIAdminUserV2Table.tsx`) | **every one of the 21 `<th>` measures exactly 120px.** The column declares no width, so `BAITableAstryx` falls through to `proportional(1)`, whose documented minimum is 120px; 21 × 120 = 2452px against a 1600px viewport, so the flex share never activates and every column pins to the floor. 120 − 16 cell padding = 104, − `BAINameActionCell`'s 44px title reserve = 60, and three 24px buttons with 2px gaps need 76 — so only `User Detail` renders and Edit / Deactivate / Permanently Delete all collapse into the overflow menu. Widest email measured in the cell's own font: 388px | **L** | Med | FIXED — `minWidth: 320` on the column, applied in the `customizeColumns` override that ADDS the actions rather than in `BAIUserNodes`, whose plain email column is reused by tables that render none. 320 = 200px of email text + 8 gap + four 24px buttons with 2px gaps (102) + 16 padding. The table already scrolls at 2452px, so this takes nothing from the other columns. NOTE: `BAITableAstryx` accepts-and-ignores `scroll` by PILOT-DECISION, so every route whose columns lack an explicit width has the same uniform-120px flattening; teaching the engine a content-sizing mode is the systemic fix and is deliberately NOT attempted under a QA row |
| **Q-17** | **The "cannot be undone" warning is a trailing Alert instead of danger text under the input** (`react/src/components/astryx-bui/BAIDeleteConfirmModalAstryx.tsx`). Reported as "WARNING: this cannot be undone! 이 input 하단에 나오는 것이 아니라 별도의 alert 로 추가됨. input 하단에 있는 Permanently Delete text 대신 error text color 로 warning 을 추가해야 함" | `<Text type="danger">` immediately under the input and ABOVE the option checkboxes (`git show origin/main:packages/backend.ai-ui/src/components/BAIDeleteConfirmModal.tsx`), antd `colorError` `#FF4D4F` / `#BE3D3F` | a `Banner status="error"` at y 549 — **below both option checkboxes**, i.e. after the thing it warns about — with the warning TEXT itself painted `rgb(0,0,0)` / `rgb(255,255,255)`; only the wash and icon carried the error hue. Under the input sat a monospace echo of the confirm string instead | **C** | Med | FIXED — the warning becomes `<Text color="danger">` in the input's own `VStack`, taking the slot of the code echo (redundant: the input's label already renders `Please type "Permanently Delete".`). The `Banner` is kept for the no-input variant, which has nothing to sit under. PILOT-DECISION 2's stated reason — "Astryx `Text` has no `danger` colour" — is stale: the brand theme registers `STATUS_TEXT_COLORS` mapping `color:danger` → `var(--color-error)` and emits the `TextColorMap` augmentation, and `TerminateSessionModalForProjectAdmin` already uses it. Checked against `.claude/rules/destructive-confirmation.md`: NO CONFLICT — the typed-confirm gate (`requireConfirmInput` / `confirmText` / `isActionDisabled`) is untouched. Trade recorded: the Banner was more prominent, but it rendered after the checkboxes |
| **Q-18** | **Table cell text paints over the next column** (`packages/backend.ai-ui/src/components/Table/BAITableAstryx.{tsx,css}`). Two reports: "테이블 리사이즈 하려고 했는데 … 오버랩되는 텍스트 발견" and "Nested Table 에서 text overflow 발생. 예시: session scheduling history" | antd clipped both faces unconditionally — `.ant-table-cell { overflow: hidden }` in `BAITable`'s `resizableTable` block matched `<th>`, `<td>` and `.ant-table-cell-fix-left` alike (`git show origin/main:…/BAITable.tsx`). Where it did not clip it WRAPPED; antd never let a cell paint sideways | Astryx DOES clip at the cell (`overflowStyles.cell` sets `overflow:hidden; text-overflow:ellipsis; white-space:nowrap; max-width:0` under the `textOverflow="truncate"` this component already requests) — but two of its own plugins re-declare `overflow: visible` on that cell so their decoration can bleed out: `useTableStickyColumns` for the pinned-column shadow (`<td>`) and `useTableColumnResize` for the full-height drag handle (`<th>`). Only `overflow` is cancelled; `white-space: nowrap` and `max-width: 0` survive, so content has a non-wrapping zero-width box with nothing clipping it. Measured: nested `step` cell (pinned) escaped **+30px** onto `result` while an identically sized non-pinned cell with **255px** of overflow escaped **0**; `/agent`'s pinned `row_id` escaped +4.2px; dragging `Session Name` to 60px put the `<th>` **+41px** over the `Status` header | **C** + scoped CSS | **Med** | FIXED — two mechanisms, two fixes. **Body:** `renderCell` wraps its content in the same clip span the header path has used since ticket 25, so the `<td>` keeps `overflow: visible` and the pinned shadow survives. **Header:** the existing header wrapper was already there but neutered on SORTABLE columns, where Astryx nests it inside the sort button's label span — a flex item whose initial `min-width: auto` floors it at min-content; one element-selector rule in `BAITableAstryx.css` sets `min-width: 0`. Measured after: `/agent` 0 escaping cells, drag escape **+41.0px → −16.6px**, nested `step` clipped, `<th>` still `overflow: visible` |
| **Q-19** | **`SessionActionButtons` reads as oversized** (`react/src/components/ComputeSessionNodeItems/SessionActionButtons.tsx`). Reported as "SessionActionButtons 의 크기가 너무 큼" | antd `size="large"`: `controlHeightLG` **40px** box with the icon at `onlyIconSize: 'inherit'` = `fontSizeLG` = **16px** (oracle) | The BOX is not the problem — Astryx `lg` renders **36px**, i.e. 4px SMALLER than antd. The GLYPH is: Astryx hard-codes `lg -> 20px` in `Button`'s StyleX, so the glyph-to-box ratio went 0.40 -> **0.56**, and four joined in a `ButtonGroup` read as one heavy slab | **C** | Med | FIXED — `toAstryxSize` maps `'large'` to Astryx **`md`**, whose glyph is exactly antd's 16px. No theme lever exists (`astryx component IconButton` exposes no glyph-size prop; the value is a StyleX literal), and Astryx has no size with antd's 40-box/16-glyph pair. Residue: 32px box vs antd's 40. Only two call sites exist and neither passes `'medium'`, so collapsing large and medium onto `md` loses no live distinction. NOT changed, and worth a separate decision: antd's non-primary buttons were `colorBgContainer` + a 1px outline where Astryx `secondary` is a solid fill — the other half of the "heavy" impression, with no `outlined` variant to map onto |
| **Q-20** | **Header-band controls hover to an opaque near-black block on the brand-orange band** (`react/src/components/MainLayout/WebUIHeader.tsx`, `packages/backend.ai-ui/src/theme-shim/antdParity.ts`). Surfaced while checking "헤더 다크에서는 검은색으로 나와야하는데 안나오네요" — the band COLOUR itself is not a regression (legacy painted `#E88A28` in dark too, from `theme.json`'s `Layout.headerBg`), but the hover is | Legacy wrapped the band in `ReverseThemeProvider`, which merged the OPPOSITE mode's seed and algorithm: app light -> reversed dark -> `colorBgTextHover` `#262626`; app dark -> reversed light -> antd default `rgba(0,0,0,0.06)` | `linear-gradient(rgb(38,38,38), rgb(38,38,38))` in BOTH modes. Every band control sits inside `MediaTheme mode="dark"` (the band is a dark surface in both modes), so `--color-overlay-hover: light-dark(rgba(0,0,0,0.06), #262626)` always resolves to the opaque branch. Residue of Q-8, which scoped its white-wash override to `button variant:primary|destructive` — ghost buttons kept the global value, which is antd-correct on a `#141414` page but not on the accent band | **T** → **C** | Med | FIXED — `ANTD_REVERSED_BAND_OVERLAYS` in the theme shim, indexed by the APP mode in JS at the band root. `light-dark()` CANNOT express this and the attempt is recorded in both files: a custom property holding `light-dark(a, b)` is substituted at USE time by the consuming element, and every consumer is inside the forced-dark subtree, so the dark slot wins in both app modes no matter which element declares it — measured `rgba(0,0,0,0.06)` in light AND dark. Legacy's `ReverseThemeProvider` also picked its token set in JS. After: light `#262626`, dark `rgba(0,0,0,0.06)` — exactly legacy |
| **Q-21** | **Every `/admin/settings` dialog is ~4.8x wider than its content** (`packages/backend.ai-ui/src/components/BAIModal.tsx`). Reported as "/admin/settings 페이지의 모든 모달 크기가 지나치게 크네요" | antd's `width="auto"` was shrink-to-fit — but only because `BAIModal` defaulted `centered` to true and antd's `.ant-modal-centered .ant-modal` is `display: inline-block` on top of `width: auto` | Overlay Network **1440 x 232** and Scheduler **1440 x 364**, against content that measures 301 and 288 — 1440 is exactly the `max-width: 90vw` clamp. `centered` is one of this component's accepted-and-ignored props (PILOT-DECISION 2), and dropping it silently took the shrink-to-fit with it: on a native `<dialog>` the UA stylesheet keeps `inset-inline: 0` and Astryx keeps `margin: auto`, so `width: auto` resolves against the VIEWPORT | **C** | **High** | FIXED — `BAIModal` translates `width: 'auto'` to `fit-content`, the CSS keyword that means what antd's `auto` meant here; `Dialog.width` documents "any CSS value" and the 90vw cap still applies. Fixed in `BAIModal` rather than at the three call sites (`OverlayNetworkSettingModal`, `SchedulerSettingModal`, `MyKeypairInfoModalLegacy`) so antd's prop semantics stay intact, which is that file's whole contract. After: **301** and **288** — the content width exactly |
| **Q-22** | **The Terms of Service / Privacy Policy documents render as an unnumbered, unindented, unspaced wall of text with invisible links** (`react/src/components/documentProse.css`, `TermsOfServiceModal.tsx`, `PrivacyPolicyModal.tsx`). Reported as "약관, 개인정보 보호 모달 쪽 텍스트 스타일이 다 깨지네요" | The UA stylesheet — `origin/main` imported no reset at all and antd 6's base rules are scoped to `[class^="ant-"]`, so for these two injected documents the UA WAS the type ramp: h1 32/700 margin 21.4, h2 24/700, p 16px margin 16, `ol` decimal with `padding-inline-start: 40px`, `a` `#0000ee` underlined | `react/src/index.css` imports `@astryxdesign/core/reset.css`, whose `@layer reset` sets `:where(h1…h6, p, …) { margin: 0 }`, `:where(ol, ul, menu) { list-style: none; margin: 0; padding: 0 }` and `:where(a) { color: inherit; text-decoration: inherit }`. Correct for a component library — `SideNav` / `List` / `TreeList` depend on it — but nothing restores block rhythm or list semantics, and these modals are the app's only un-classed third-party HTML. Measured: **every** margin 0, `ol`/`ul`/`li` `list-style: none`, padding 0, links unstyled, across 200 `<li>` | scoped CSS | **High** | FIXED — a scoped `.bai-document-prose` sheet, the skill's "last resort, justified in the diff" rung: the elements are strings from a fetched document, so no prop can reach them, and it MUST stay scoped or a global `ol { list-style: decimal }` would fight every Astryx list surface. Values are on the spacing scale (P19), not the UA's `em`-derived px. After: headings 20/12, paragraphs 12/12, `ol` decimal with a 40px gutter, links accent + underlined. RESIDUE: a `<ul>` NESTED inside an `<li>` still computes `list-style-type: none` — a rule census on the live element finds only the reset's zero-specificity `:where()` and this rule reaching it, so this one should win, and does for `padding-inline-start` on the very same selector; shorthand and longhand were both tried and re-measured. The documents use `<ol>` for all 200 numbered items, so the unmarked nested `<ul>` is cosmetic and is recorded rather than guessed at |
| **Q-25** | **Start-page announcement: the Edit / Dismiss buttons are vertically centred instead of top-right** (`react/src/components/AnnouncementAlert.tsx`) | antd rendered this as `ant-alert-with-description`, whose style is `alignItems: 'flex-start'` for icon, content, actions and close alike | header `align-items: center`, end-area top **+37.9px** below the content top on a 123.8px banner. `Banner` computes `isSingleLine = description == null && hasActions`, and this call site put the whole multi-line markdown into `title` with NO `description`, so a 100px body was classified single-line | **C** | Med | FIXED — the `<Markdown>` moves from `title` to `description` with `title={null}`, so `isSingleLine` is false and the header keeps Astryx's own `flex-start` — which IS the antd `-with-description` geometry. `Banner` has no `className`/`xstyle` escape hatch, so this is the only clean rung. After: end-top −4px (Banner's own end-slot inset), banner 117.9px. Residue: the body now renders through Banner's `description` ramp at 12px vs legacy's 14px; hand-setting a font size at this call site is exactly the Banner-box styling POLISH-3 removed, so it is recorded rather than overridden |
| **Q-26** | **Folder-create modal labels are bunched at the centre** (`react/src/components/FolderCreateModalV2.tsx`) | legacy carried a `createStyles` block — `.ant-form-item-label { display: flex; align-items: start; padding-left: token.paddingSM }` — i.e. LEFT-aligned labels inset 12px | the label column is 33.3% (correct, matching legacy `labelCol={{span:8}}`), but labels are `text-align: end`, leaving **59.3 / 95.3 / 96.3 / 119.3 / 143.3px** of dead space between each label's text and its control. The migration dropped the `createStyles` block and the form engine's default `text-align: end` took over | **L** | Med | FIXED — `labelAlign="left"` on the `<Form>`; the engine already supports it (`data-align="left"` → `text-align: start`). Dead space **0 for all five**, both modes. Residue: legacy's 12px label-column inset is not re-added — the Astryx form now sits in a `VStack` with `paddingLeft/Right: token.paddingMD`, so the effective inset is 16px vs legacy's 12. ALSO FOUND: `react/src/components/FolderCreateModal.css` re-points the same legacy rule at `[data-bai-form-item-label]` (the inner `<label>`) instead of `…-label-col`; it does reproduce the inset, but `FolderCreateModal.tsx` has **zero importers** — dead code |
| **Q-27** | **The RBAC detail drawer resets to an empty "RBAC Role Info" shell while closing** (`react/src/components/RoleDetailDrawer.tsx`) | legacy had a STATIC `title={t('rbac.RoleDetailInfo')}` and the same `{role && …}` body, so it blanked the body too — but the header text never changed. The migration moved the role name into the title, which is what makes the reset legible | rAF recorder: the fallback title and a 0-char body appear at **t=182ms**, while the drawer is still fully open; the slide-out only starts at t=597ms. **4 frames empty-while-fully-open.** `RBACManagementPage` drives `open` and `roleFrgmt` from the same URL param, so the fragment ref nulls in the same commit | **C** | Med | FIXED — the drawer keeps the last non-null fragment ref for the duration of the exit. `BAIUnmountAfterClose` is a documented NO-OP here (`BAIDrawerAstryx` exposes no `afterClose`/`afterOpenChange`) and the skill's id-state alternative would kill the exit animation, so the fix is drawer-local. The prescribed `useRef` form fails `react-hooks/refs` — that rule describes precisely this bug — so it uses React's documented adjust-state-while-rendering form instead; it cannot loop, because within one parent commit `roleFrgmt` is a fixed prop. After: **0** empty frames, last sampled frame before removal still carries the role's title and a 342-char body. Role-switch verified: closing A then opening B shows B, never A |
| **Q-28** | **The folder-explorer upload dropdown opens on click and immediately closes itself** (`packages/backend.ai-ui/src/components/baiClient/FileExplorer/ExplorerActionControls.tsx`) | antd's `onOpenChange(open)` fires ONCE per state change, so passing a toggler happened to work (`trigger={['click']}`, `onOpenChange={toggleUploadDropdown}`) | Not a single self-close — an **infinite oscillation**: over a 2s/40-sample window the open flag read `1110011011010100…`. `useToggle`'s `toggle` ignores its boolean argument, and Astryx's `DropdownMenu` calls `onOpenChange` TWICE per transition (once from the button click handler, once from the popover lifecycle), so each notification flips the state again. The keyboard path calls `popover.show()` directly and fires once — which is exactly why Enter worked and click did not | **C** | Med | FIXED — `set` instead of `toggle`, passed straight to `onOpenChange`; `useToggle`'s `set(value)` is idempotent, so the double-fire becomes a no-op. The post-file-pick call becomes `set(false)`. After: **40/40 samples open**, `aria-expanded` true throughout; second click closes, Enter still opens, Escape still closes. Grep sweep: no other `onOpenChange={toggle…}` exists in either tree — the two other controlled `DropdownMenu`s already pass `useState` setters. NOT exercised: the post-file-pick path, which would start a real upload against the shared cluster |
| **Q-29** | **Create Project: the Registry and Project inputs are stacked instead of sharing a row** (`packages/backend.ai-ui/src/components/fragments/BAIProjectSettingModal.tsx`). Reported as "Create Project 모달에서 Container Registry for Image Commit 의 input (레지스트리, 프로젝트) 가 같은 라인에 일렬로 오도록 수정 필요" | two antd `<Input style={{width:'calc(50% - 8px)'}}>` siblings; antd's `Input` renders an inline-block `<input>`, so two shared one line | fields at y916 and **y948** — 32px apart. Their WIDTHS are already correct (228px each = `calc(50% - 8px)`); the flow is wrong. The migration swapped `Input` for `AstryxFormTextInput`, which renders Astryx's block-level `.astryx-field` wrapper, and two block-level siblings stack regardless of width | **L** | Med | FIXED — an `HStack gap={2} align="start" width="100%"` around both `Form.Item noStyle`, both inputs `width="100%"`, and the now-redundant margin hack dropped (with the unused `theme.useToken()` import). After: both at y916, ΔY **0**, 8px gutter, 0px overflow past the control column. `HStack` was enough — no `minWidth: 0` needed, since `.astryx-field` already computes it, so the `Grid`-child trap (`issues/p3-w2a.md` W2A-17) does not apply |
| **Q-31** | **The supplementary-GID field only splits on Enter, though the UI copy promises comma and space** (`react/src/components/astryxFormControls.tsx`) | `<BAISelect mode="tags" tokenSeparators={[',', ' ']}>` — antd split on the keystroke and on paste. The promise is still in the UI: `resources/i18n/en.json` "Enter GIDs separated by commas or spaces" | typing `10,20 30` produced **zero** tokens and left the raw string in the input. `splitOnSeparators` already existed in the wrapper, but inside `onChange`, which Astryx fires only when `hasCreate` commits — i.e. separators were honoured on COMMIT, never while typing. Astryx exposes no delimiter prop (`astryx component Tokenizer` lists none; the shipped `TokenizerHandle` has only `focus`/`blur`) | **C** | Med | FIXED — team-approved synthetic-key route: a ref on the `Tokenizer` root plus a capture-phase `keydown` that, on a configured separator with a non-empty input, `preventDefault()`s and re-dispatches a synthetic `Enter` on the same `<input>` so Astryx's own create path commits. `debounceMs={0}` was REQUIRED and is the non-obvious half: `BaseTypeahead` debounces its search 150ms and gates its Enter branch on `popover.isOpen`, so at 120ms/key the synthetic Enter hit a closed popover, did nothing, **and the cancelled separator was lost** — strictly worse than before. Two non-vacuous unit tests (verified failing with the listener disabled) plus a live pass on the real modal. Residue, deliberate: `10,20 30` yields two tokens plus `30` pending — antd behaved identically, since `tokenSeparators` cuts on the separator. The comment names the non-trusted-event dependency as the class of thing that breaks silently on an Astryx bump, and records the standing upstream ask |
| **Q-32** | **Bulk Create Users: the e-mail prefix and suffix fields collide instead of reading as one control** (`packages/backend.ai-ui/src/components/BAICompactGroup.{tsx,css}` — new — and `react/src/components/UserSettingModal.tsx`) | antd `Space.Compact` welded them: `-1px` between, squared inner corners, one shared border, focused edge on top | the two bordered fields touch at exactly x=800 with **each still carrying `border-radius: 8px`**, because the migration kept `Space.Compact`'s gap (0) but not its weld — Astryx has no weld for arbitrary form controls | **C** | Med | FIXED — team-approved: a new `BAICompactGroup` wraps `HStack` with `gap`/`wrap` fixed and welds through a co-located sheet in `@layer components`, reaching children by stable `.astryx-*` classes per the `BAITabList.css` precedent. Measured against antd exactly: gap **−1**, prefix radii 8/0/0/8, suffix 0/8/8/0, focused z-index **2**. The key finding, recorded in the file header: Astryx already ships this recipe in `InputGroup`'s `groupStyles` but hands it out via `InputGroupContext`, and `InputGroup` takes ONE group-level label and documents "Don't put multiple text inputs in one group" — so it cannot host two `BAIFormItem`s that each own a label, `rules` and an error slot. z-index had to be 2, not Astryx's 1, because its field surfaces already compute `z-index: 1`. Ships with 6 CSF3 stories and 4 tests. NOT restored, needs a team call: legacy's inline `@` adornment on the suffix — `InputGroupText` would render it but reintroduces the single-label constraint |
| **Q-33** | **Every session-app launcher icon is squashed 2.25x horizontally** (`react/src/components/ComputeSessionNodeItems/AppLauncherModal.tsx`) | `<BAIButton style={{height:72,width:72}} icon={<Image style={{height:36,width:36}}/>}>`; antd's `.ant-btn-icon` is content-sized, so the image rendered **36x36, ratio 1.000** | every icon renders **16x36** from a 1:1 source (ttyd 24x24, jupyter 346x346, jupyterlab 400x400, vscode 256x256) — ratio **0.444** | **C** | **High** | FIXED — and the first diagnosis was only half the cause. `flexShrink: 0` alone re-measured **still 16x36**: the binding constraint is Astryx's `reset.css` `:where(img, video) { max-width: 100%; height: auto }`, and since `Button` puts the icon in a fixed 16x16 wrapper, "100%" IS 16px — `max-width` beats `width` regardless of specificity, which is exactly why the box was 16 WIDE and 36 TALL. `flex-shrink: 1` was a genuine second, independent constraint producing the same 16px; both had to go. Final: `{height:36, width:36, maxWidth:'none', flexShrink:0, objectFit:'contain'}`. After: **36x36, ratio 1.000** on all four apps, both modes |
| **Q-34** | **A search-less `Selector` opens its panel OVER its own trigger, hiding the field label and the current value** (`BAISelect.tsx`, `astryxFormControls.tsx`, `VFolderNodeDescription{,V2}.tsx`, `BAIDynamicUnitInputNumber.tsx`). Reported three times: the folder mount-permission select, the session-launcher cluster mode, and "Select 에서 검색 기능이 없는 경우, 선택된 아이템을 확인할 수 없습니다" | antd `Select`'s default placement is `bottomLeft` with `offset: [0, 4]`, flipping to `topLeft` only when it does not fit. **antd never drew the dropdown over its trigger** | `Selector.tsx` sets `shouldOverlaySelectedItem = placement == null && !hasSearch` and applies a negative `margin-block-start` that centres the SELECTED OPTION over the trigger (macOS-style): measured **−65.97px** on the launcher's memory-unit selector and **−29.98px** on the page-size selector, i.e. panel top 60.97px ABOVE the trigger bottom. Search-enabled selectors were already correct at +5px | **C** | **High** | FIXED at the six reachable sites with `placement="below"` — the documented opt-out (`popoverPlacement = placement ?? 'below'`, offset forced to 0), written BEFORE the prop spread at the two shared wrappers so an explicit caller value still wins. Note `astryx component Selector` does NOT list `placement`; it is in the TS source only, and `MultiSelector` already hard-codes it. After: **+5.00** on the launcher unit selector, the folder mount-permission selector and the form selector — 1px off antd's 4px offset. `BAISelect`'s pin is a deliberate no-op today (`hasSearch: showSearch !== false`, and zero call sites pass `showSearch={false}`). **NOT CLOSED — every `BAITable` page-size dropdown app-wide.** Astryx's own `Pagination` hard-codes its internal `Selector` with no `placement` and no pass-through, confirmed at source; it measured −28.98 unchanged in the same after-run, which doubles as the standing control. Closing it needs `astryx swizzle Pagination` or a scoped `!important` on the inline margin — both are judgement calls, so it is reported rather than taken |
| **Q-35** | **The session status tag is clipped mid-glyph on every row** (`react/src/components/SessionNodes.tsx`) | the legacy column definition is **byte-identical** — no `width`, no `minWidth` — so the regression is entirely in the engine's fallback | the column gets a strict 1/N equal share: **120px** on `/admin-session` at every width and on `/session` below 1600. Content box 104px against a needed 124 (worst real cell: `PENDING` + `#n` + "Queue Position"), and cells are `overflow: hidden; text-overflow: clip` | **L** | **High** | FIXED — `minWidth: 140` (124 content + 8/8 padding), which `BAITableAstryx` forwards into `proportional(1, {minWidth})`. After: 0 clipping on `/session` and `/admin-session` at 1600 and 1200. `/session`@1600 had been at exactly 124/124, one glyph from clipping, and now has headroom |
| **Q-36** | **Session Type text and its info icon are 4px out of vertical centre** (`react/src/components/SessionDetailContent.tsx`) | antd's `.ant-descriptions-item-content` was `display: table-cell`, i.e. the same baseline layout — so this is **pre-existing in legacy, not a migration regression** | badge centre-y 238.84 vs icon-button centre-y 234.84, delta **4.00px**. The `<dd>` is `display: block` and both children are inline-flex on the baseline. The Status row directly above already wraps its identical pair in `BAIFlex` and measures 0.00 | **L** | Low | FIXED — the pair is wrapped the same way. A bare `<BAIFlex>` was used rather than `align="center" gap="xs"`, because the Status row being mirrored is itself bare (`BAIFlex` defaults to `align='center'`, `gap=0`) and matching it exactly keeps the two rows' horizontal rhythm identical. After: delta **0.00**, both modes. The info button only renders for a batch session with a `startup_command`, which this cluster has none of, so the DOM shape was reproduced client-side only and removed — no cluster mutation |
