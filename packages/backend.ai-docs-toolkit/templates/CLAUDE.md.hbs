<!-- Generated by docs-toolkit. You may customize this file. -->
<!-- Re-run `docs-toolkit agents` to regenerate from template. -->

# {{projectTitle}} User Manual

This package contains the official user manual for {{projectTitle}}. It is a multilingual documentation system supporting {{#each languages}}{{label}}{{#unless @last}}, {{/unless}}{{/each}}.

## Documentation Structure

```
src/
├── book.config.yaml          # Navigation config for all languages
{{#each languages}}├── {{code}}/                        # {{label}}
{{/each}}```

Each language directory contains:
```
{lang}/
├── *.md                       # Documentation pages
└── images/                    # Screenshots
```

## AI Agent Workflow

Four agents handle the documentation lifecycle:

1. **docs-update-planner** - Analyzes PR changes or feature descriptions to create a documentation update plan (`.agent-output/docs-update-plan-{topic}.md`)
2. **docs-update-writer** - Writes documentation content across all {{languages.length}} languages following the plan
3. **docs-update-reviewer** - Reviews for accuracy, consistency, style, and translation quality; auto-fixes issues (`.agent-output/docs-review-report-{topic}.md`)
4. **docs-screenshot-capturer** - Captures screenshots using Playwright MCP by navigating the live application, saves to all language image directories

### Agent Working Files

Agent output files (plans, review reports) are stored in `.agent-output/` (gitignored).

## Key References

Read these before writing or editing documentation:

{{#if styleGuideFile}}- @{{styleGuideFile}} - Formatting, structure, and writing conventions
{{/if}}{{#if terminologyFile}}- @{{terminologyFile}} - Standardized terminology across all languages
{{/if}}{{#if translationGuideFile}}- @{{translationGuideFile}} - Language-specific translation rules and workflows
{{/if}}{{#if screenshotGuideFile}}- @{{screenshotGuideFile}} - Screenshot capture, naming, and maintenance
{{/if}}

## Configuration

- **Navigation**: `src/book.config.yaml` - Defines page ordering and titles for each language
- **Adding a new page**: Create the `.md` file under each language directory, then add navigation entries for all {{languages.length}} languages in `book.config.yaml`

## Quick Rules

- Always write English (`en/`) first, then translate to other languages
{{#if terminologyFile}}- Use terminology from `{{terminologyFile}}` consistently
{{/if}}{{#if styleGuideFile}}- Follow `{{styleGuideFile}}` for all formatting
{{/if}}
- Images are shared naming across languages: `![](images/filename.png)`
- Keep all {{languages.length}} language versions structurally identical
