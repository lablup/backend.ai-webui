/**
 @license
 Copyright (c) 2015-2026 Lablup Inc. All rights reserved.

 Tokens with NO Astryx counterpart (to-astryx ticket 03, from the ticket-06
 spike).

 These 19 names are ours to own. The values below are Ant Design's *current*
 computed values for this repo's theme (resources/theme.json seed, default +
 dark algorithm), captured with `theme.getDesignToken()` during the ticket-06
 spike, so day-1 parity is exact and every later change is a deliberate
 design decision rather than an accident of the migration.
 */
/** [light, dark] — the same tuple shape Astryx uses for its own tokens. */
export declare const SELF_TOKENS: Record<string, [string | number, string | number]>;
