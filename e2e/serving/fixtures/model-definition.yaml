models:
  - name: "mock-openai"
    model_path: "/models"
    service:
      start_command:
        - python3
        - /models/mock_openai_server.py
      port: 8000
      health_check:
        path: /health
        max_retries: 10
