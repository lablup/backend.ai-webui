{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "definitions": {
    "seedValue": {
      "description": "A color seed: a string applies to both schemes, a [light, dark] tuple splits them (Astryx TokenValue semantics).",
      "oneOf": [
        { "type": "string" },
        {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 2,
          "maxItems": 2
        }
      ]
    },
    "logoSize": {
      "type": "object",
      "properties": {
        "width": { "type": "number" },
        "height": { "type": "number" }
      }
    }
  },
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": {
      "const": 2,
      "description": "Appearance document format version. v1 (antd-shaped light/dark documents) is not readable since FR-1964 — see the theme migration guide."
    },
    "theme": {
      "type": "object",
      "description": "The Astryx-vocabulary half: everything that feeds the theme pipeline.",
      "properties": {
        "fontFamily": {
          "type": "string",
          "description": "The font family applied to both schemes."
        },
        "siderMode": {
          "type": "string",
          "enum": ["dark", "light"],
          "description": "Forces the sider chrome's polarity regardless of the site's dark mode status. Omit to follow the page scheme."
        },
        "families": {
          "type": "object",
          "description": "Selectable theme families keyed by family id. 'default' must exist; other entries appear in the family selector with their branding.familyLabels label.",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "seeds": {
                "type": "object",
                "description": "Brand color seeds. Roles derive from these: brand uses accent, admin uses info, secondary uses success.",
                "properties": {
                  "accent": { "$ref": "#/definitions/seedValue" },
                  "link": { "$ref": "#/definitions/seedValue" },
                  "info": { "$ref": "#/definitions/seedValue" },
                  "error": { "$ref": "#/definitions/seedValue" },
                  "success": { "$ref": "#/definitions/seedValue" },
                  "warning": { "$ref": "#/definitions/seedValue" }
                }
              },
              "headerBg": {
                "$ref": "#/definitions/seedValue",
                "description": "Header band background color."
              }
            }
          },
          "required": ["default"]
        }
      }
    },
    "branding": {
      "type": "object",
      "description": "Structural metadata no theme token can express.",
      "properties": {
        "logo": {
          "type": "object",
          "description": "Properties to customize the site's logo, considering both light and dark modes.",
          "properties": {
            "src": {
              "type": "string",
              "description": "The source of the logo image."
            },
            "srcCollapsed": {
              "type": "string",
              "description": "The source of the collapsed logo image."
            },
            "srcDark": {
              "type": "string",
              "description": "The source of the logo image in dark mode."
            },
            "srcCollapsedDark": {
              "type": "string",
              "description": "The source of the collapsed logo image in dark mode."
            },
            "alt": {
              "type": "string",
              "description": "The alt text for the logo image."
            },
            "href": {
              "type": "string",
              "description": "The URL that the logo links to."
            },
            "size": { "$ref": "#/definitions/logoSize" },
            "sizeCollapsed": { "$ref": "#/definitions/logoSize" },
            "loginLogoSrc": {
              "type": "string",
              "description": "The login form logo for light mode. Falls back to srcDark, then the default path."
            },
            "loginLogoSrcDark": {
              "type": "string",
              "description": "The login form logo for dark mode. Falls back to src, then the default path."
            },
            "loginLogoSize": { "$ref": "#/definitions/logoSize" },
            "aboutLogoSrc": {
              "type": "string",
              "description": "The about modal logo for light mode. Falls back to srcDark, then the default path."
            },
            "aboutLogoSrcDark": {
              "type": "string",
              "description": "The about modal logo for dark mode. Falls back to src, then the default path."
            },
            "aboutLogoSize": { "$ref": "#/definitions/logoSize" },
            "aboutModalSize": {
              "$ref": "#/definitions/logoSize",
              "deprecated": true,
              "description": "Deprecated. Use aboutLogoSize instead."
            }
          }
        },
        "companyName": {
          "type": "string",
          "description": "The name of the company or organization."
        },
        "brandName": {
          "type": "string",
          "description": "The name of the brand or product."
        },
        "familyLabels": {
          "type": "object",
          "description": "Family selector labels keyed by family id; falls back to the id.",
          "additionalProperties": { "type": "string" }
        }
      }
    }
  },
  "required": ["schemaVersion"]
}
